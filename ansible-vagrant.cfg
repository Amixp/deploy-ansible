[defaults]
remote_user = vagrant
remote_port = 22
private_key_file = ~/.ssh/vagrant
hostfile = inventory/vagrant
host_key_checking = False
log_path = logs/vagrant.log

[ssh_connection]
control_path = %(directory)s/%%h-%%r
